# Текущая формальная граница

75 принятых экспортов 11 новых LB-модулей; полное локальное импортное
замыкание — 19 исходников. Точный перечень и SHA — FORMAL_INDEX.json,
FORMAL_*_AUDIT.json, clean_build_0900/BUILD_RESULT.json.

После среза 08:30 добавлены:

- LowerBoundPaidProjection: 17 экспортов. Реальная проекция L/R и X,
  сохранение цены и обязательных посещений в тех же проекциях, все
  отражённые фазы, нечётная поправка, общий предел r≥floor((a²+b²)/2)≥ab.
  Эквивалентность X с буквальной физической транспозицией доказана в обе
  стороны; стоимость каждой буквы учитывается.
- LowerBoundSwapBudget: 6 экспортов. Приращение инверсий за X не больше1,
  число конечных инверсий вычислено для любого размера, затем выведен
  минимальный X-бюджет. Главный результат physical_two_block_total_lower:
  **любая такая физическая двухблочная трасса имеет полную длину≥N**.
  Нет предпосылки монотонности или заранее заданного числа X.

Новый полный [семантический аудит](SEMANTIC_AUDIT_0900_RU.md) объясняет,
какие именно состояния и шаги формализованы и почему модель не сужена.

**Общий D_n≥N ещё не является одной end-to-end Lean-теоремой.**
Аналитическим остаётся соединение структурной редукции настоящего
BlockExchange.run-слова с индексным представлением PhysicalTrace, а также
сборка вырожденных случаев классов0/1 с основной веткой. В настоящее время
общая нижняя аналитически независимо проверена, а основной двухблочный
случай со всеми ценами полностью проверен kernel в физической модели.

Исторические FORMAL_SCOPE_0710/0800/0830 сохранены как срезы прошлого
состояния. Их перечисления незакрытых звеньев не описывают текущий объём.
Все принятые зависимости — propext, Classical.choice, Quot.sound;
нет sorryAx, native_decide, добавленных аксиом или предположенного r≥ab.
